// set up the menu graphics.

#include <graphics.h>
int main()
{
    int gd = DETECT, gm;
    initgraph (&gd, &gm, "");
    int x = 0;
    int y = 0;
    int font = 10;
    int direction = 0;
    int font_size = 30;
    settextstyle (font, direction, font_size);
    outtextxy(x, y, "4-SQUARE");
    getch();
    closegraph();
    return 0;
}
initgraph;

void setfillstyle(int pattern, int color)
void floodfill(int x, int y, int border_color)

// draw the menu graphics.

pattern = HATCH_FILL, Color = RED
circle : x = 500, y = 500, radius = 200

// make the text.

void settextstyle(int font, int direction, int font_size);

// check for inputs.

while(1)
{
    // check for the controller input.

    padCheck(Pad1Up):
    ptr = fopen(\MENUS\GAME.c);

}

// end of main menu code.